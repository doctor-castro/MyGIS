package geometry;

//Just extended the basic geometry using the giveArea interface made in classromm
public interface flatShapes extends BasicGeometry{
	
	//just needed to add the area for these figures. Other stuff already in Basic Geometry 
	public double giveArea() ;
	
}
